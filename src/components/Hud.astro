---
// (no props — record count lives in the Ticker on the right side of the globe)
---

<header class="top">
  <a href="/" class="brand">
    <span class="logo">UFOS</span>
    <span class="logo-sub">/ ET PHONE HOME</span>
  </a>
  <span class="clock" id="clock">UTC --:--:--</span>
  <div class="hud-right">
    <span class="stamp">DECLASSIFIED · RELEASE_01</span>
    <nav class="primary-nav" aria-label="Site nav">
      <a href="/about" class="nav-about"><span class="nav-glyph">ⓘ</span>ABOUT</a>
    </nav>
  </div>
</header>

<style>
  .top {
    position: fixed;
    top: 0; left: 0; right: 0;
    height: 56px;
    display: flex;
    align-items: center;
    padding: 0 34px;
    background: linear-gradient(180deg, rgba(0,0,0,.6) 0%, rgba(0,0,0,0) 100%);
    border-bottom: 1px solid var(--color-line);
    gap: 18px;
    backdrop-filter: blur(4px);
    z-index: 7;
  }
  .brand {
    display: flex;
    align-items: baseline;
    gap: 10px;
    text-decoration: none;
  }
  /* "UFOS" stays the HUD green; "/ ET PHONE HOME" glows purple. */
  .logo {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 18px;
    letter-spacing: .24em;
    color: #b8ffe0;
    text-shadow:
      0 0 8px rgba(106,255,200,.9),
      0 0 18px rgba(106,255,200,.55),
      0 0 36px rgba(106,255,200,.3);
  }
  .logo-sub {
    font-size: 12px;
    letter-spacing: .3em;
    color: #e0c8ff;
    text-shadow:
      0 0 8px rgba(199,148,255,.9),
      0 0 18px rgba(168,99,255,.6),
      0 0 34px rgba(140,80,255,.35);
  }
  .clock {
    font-size: 10px;
    letter-spacing: .15em;
    opacity: .65;
    margin-left: 8px;
    font-variant-numeric: tabular-nums;
    color: #cdd3dc;
  }
  .hud-right {
    display: flex;
    align-items: center;
    gap: 14px;
    margin-left: auto;
  }
  .primary-nav {
    display: flex;
    align-items: center;
    gap: 14px;
  }
  .nav-label {
    font-size: 9px;
    letter-spacing: .25em;
    color: rgba(106,255,200,.55);
    padding-right: 4px;
    border-right: 1px solid rgba(106,255,200,.2);
    text-transform: uppercase;
  }
  .primary-nav a {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 10px;
    letter-spacing: .2em;
    color: rgba(232,237,243,.6);
    text-decoration: none;
    transition: color .15s;
  }
  .primary-nav a:hover { color: var(--color-hud); }
  .nav-glyph {
    font-size: 11px;
    color: rgba(106,255,200,.7);
    line-height: 1;
    letter-spacing: 0;
  }
  .primary-nav a:hover .nav-glyph { color: var(--color-hud); }
  .stamp {
    font-size: 9px;
    letter-spacing: .2em;
    color: var(--color-vid);
    border: 1px solid rgba(255,59,59,.55);
    padding: 4px 10px;
    border-radius: 2px;
    background: rgba(255,59,59,.06);
    text-transform: uppercase;
    font-weight: 700;
  }

  /* Tablet */
  @media (max-width: 1024px) {
    .top {
      gap: 12px;
      padding: 0 20px;
    }
    .logo-sub { font-size: 10px; letter-spacing: .25em; }
    .clock { margin-left: 0; }
    .primary-nav { gap: 10px; }
    .nav-label { display: none; }
    .stamp { font-size: 8px; padding: 3px 7px; letter-spacing: .15em; }
  }

  /* Mobile — show glyphs only for nav links; keep the DECLASSIFIED stamp */
  @media (max-width: 767px) {
    .top {
      height: 48px;
      padding: 0 14px 0 22px;
      gap: 6px;
    }
    .brand { gap: 5px; flex-shrink: 1; min-width: 0; }
    .logo { font-size: 12.5px; letter-spacing: .16em; }
    .logo-sub { font-size: 8.5px; letter-spacing: .18em; }
    .clock { display: none; }
    .nav-label { display: none; }
    .hud-right { gap: 8px; }
    .stamp {
      font-size: 7px;
      padding: 2px 5px;
      letter-spacing: .1em;
      flex-shrink: 0;
      white-space: nowrap;
    }
    .primary-nav { gap: 10px; }
    .primary-nav a {
      font-size: 0;
      letter-spacing: 0;
      gap: 0;
    }
    .nav-glyph {
      font-size: 15px;
      color: rgba(106,255,200,.9);
    }
  }

  /* Very small mobile — drop the stamp's "RELEASE_01" half if things get tight */
  @media (max-width: 400px) {
    .logo-sub { font-size: 8px; letter-spacing: .14em; }
    .stamp { font-size: 6.5px; padding: 2px 4px; }
    .hud-right { gap: 6px; }
  }
</style>

<script>
  function tick() {
    const el = document.getElementById('clock');
    if (!el) return;
    el.textContent = `UTC ${new Date().toISOString().slice(11, 19)}`;
  }
  tick();
  setInterval(tick, 1000);
</script>
