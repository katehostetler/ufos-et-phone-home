node_modules/
dist/
.astro/
# pdf.js runtime assets (wasm decoders, ICC profiles, standard fonts) — copied
# out of node_modules/pdfjs-dist by scripts/copy-pdfjs-assets.mjs on postinstall.
public/pdfjs/
.vercel/
.output/
.cache/

# brainstorming artifacts (visual companion mockups)
.superpowers/

# local-only working docs — design specs, implementation plans, deploy/handoff
# notes. Kept on disk, not in the public repo (README + CHANGELOG cover what
# anyone cloning actually needs).
docs/

# env / secrets
.env
.env.*
!.env.example

# build-time scratch
.wargov-cookies.txt

# playwright + ad-hoc test screenshots
.playwright-mcp/
*.png
!public/**/*.png
!src/**/*.png

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
.claude/
